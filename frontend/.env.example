# Backend API URL - defaults to current domain (/)
REACT_APP_BACKEND_URL=/

# Optional: Enable visual edits for development
REACT_APP_ENABLE_VISUAL_EDITS=false

# Optional: Disable hot reload for specific environments
DISABLE_HOT_RELOAD=false

# Optional: Enable health check endpoint
ENABLE_HEALTH_CHECK=false
