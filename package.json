{
  "name": "cherviton",
  "version": "1.0.0",
  "description": "Telegram Mini App game similar to Slither.io",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm install --include=dev && npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm install --include=dev && npm run build",
    "build:frontend": "cd frontend && npm install --include=dev && npm run build",
    "start": "node backend/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
